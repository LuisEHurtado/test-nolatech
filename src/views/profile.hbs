  {{>navbar}}

<section class="signup col-12 mx-auto pt-1">
  <div class="container">
    <div class="signup-content">

      <form action="/update_profile/{{user.id}}" method="POST">
        <h2 class="form-title">Pefil <br>  {{user.first_name}} {{user.last_name}}   </h2>
        <div class="row">
          <div class="form-group col-lg-6">
            <label for="firstName">Primer nombre</label>
            <input type="text" class="form-input" name="firstName" id="firstName" placeholder="Primer nombre" value="{{user.first_name}}"  autofocus/>
          </div>
          <div class="form-group col-lg-6">
            <label for="lastName">Primer apellido</label>
            <input type="text" class="form-input" name="lastName" id="lastName" placeholder="Primer apellido" value="{{user.last_name}}" />
          </div>
          <div class="form-group col-lg-6">
            <label for="username">Usuario</label>
            <input type="text" class="form-input" name="username" id="username" placeholder="Usuario" value="{{user.username}}"/>
          </div>
          <div class="form-group col-lg-6">
            <label for="email">Correo electrónico</label>
            <input type="email" class="form-input" name="email" id="email" placeholder="Correo electrónico" value="{{user.email}}" />
          </div>
          <div class="form-group col-lg-12">
            <label for="password">Contraseña</label>
            <input type="password" class="form-input" name="password" id="password" placeholder="Contraseña" />
          </div>
         
        </div>
        <div class="form-group">
          <button class="form-submit bg-primary"> Actualizar </button>
            {{>message}} 
        </div>

      </form>
    </div>


  </div>
</section>